import { DefaultTheme } from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#7a29ff',
    secondary: '#7d7e82',
    error: '#ff5773',
  },
};
