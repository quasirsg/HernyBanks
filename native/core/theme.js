import { DefaultTheme } from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#7a29ff',
    secondary: '#414757',
    error: '#ff5773',
  },
};
